<div id="<%= dom_id(cloud) %>" class="cloud-details">
  <div class="container">
            <h2 class="headline"><%= cloud.name %></h2>
    <div class="hero-image" style="background-image: url('<%= cloud.picture_url %>');">


      </div>
        <div class="cloud-info">
    <div class="show-card"> <%= cloud.description %></div>
    </div>
        <div class="show-address"><%= image_tag("address.svg", alt: "Cloud Odyssey", height: "35") %> Set your compass to <strong><%= cloud.address %></strong></div>
    <div class="show-address"><%= image_tag("calender.svg", alt: "Cloud Odyssey", height: "35") %> The cloud graces the sky from <strong><%= cloud.available_from %></strong> until its farewell on <strong><%= cloud.available_until %></strong></div>

  </div>

  </div>
</div>
